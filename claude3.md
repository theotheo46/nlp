Напишите функцию run_active_learning для запуска цикла активного обучения. Она получает на вход массив неразмеченных данных и список классов методов активного обучения вместе с их именами. Для каждого класса функция должна запустить процесс активного обучения с самого начала и остановиться, когда бюджет на разметку закончился. На каждой итерации обучения функция логирует текущее значение F1 на тестовой выборке и стоимость разметки текущего набора текстов. На выход она возвращает словарь, в котором для каждого имени метода хранится сохраненная траектория F1 и цен, а также список индексов размеченных до израсходования бюджета текстов.

<run_active_learning>
def run_active_learning(unlabeled_data, al_classes, names):
    results = dict()

    #... TO DO

    return results
</run_active_learning>
