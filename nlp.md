Антон добрый день. Есть такая задача по n8n + flowise: 
- В чате пользователь с помощью LLM,  промпта и с использованием LLM Агента формирует объект Timer, который включает в себя ряд полей включая время срабатывания. 
- Этот объект записывается в Relation DB в виде элемента таблицы и с помощью какого то супервайзера далее на основании этой таблицы формируются уже n8n сущности вида Schedule Trigger node настроенные на данное время и потом уже они в рамках n8n срабатывают

Вопрос - Можно ли реализовать такое в n8n или там все такие Schedule Trigger node надо формировать  on design time и нельзя формировать динамически на рантайме ? Либо на n8n это требует каких то плясок с бубном и проще реализовать например на python в виде веб-приложения которое в свою очередь будет дергать ручку из Flowise для работы с LLM чатом?
